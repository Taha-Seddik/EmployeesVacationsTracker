import Box from "@mui/material/Box";
import { styled } from "@mui/material/styles";

// const Container = styled(Box)(({ theme }) => ({
//   marginTop: theme.spacing(10),
//   paddingBottom: theme.spacing(5),
// }));

export const AdminMainSide: React.FC<{}> = () => {
  return <Box></Box>;
};
